# hytale-downloader-bash

Hytale's downloader reverse engineered and implemented in Bash

## Supported Arguments

Usage of hytale-downloader-bash.sh:
  -download-path string
        Path to download zip to
  -patchline string
        Patchline to download from (default "release")
  -print-version
        Print available game version and exit

